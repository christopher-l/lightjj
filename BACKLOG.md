# lightjj Backlog

Open items only. Done-item narratives live in [docs/CHANGELOG-ARCHIVE.md](docs/CHANGELOG-ARCHIVE.md).

Last shipped: **2026-08** v1.37.2 — merge-editor line-range position model, image diffs (#20), routable `?change=`/`?revset=` URLs (#35), `lightjj api` multi-tab discovery, restored `pnpm check` gate. Unreleased on top (v1.38.0): split/unified toggle in FileHistoryPanel + FileComparePicker with `|` as the app-wide split-view key (#38), PR badges for non-`@me` same-repo PRs (author in tooltip) + "Open PR" on bookmark right-click (#36 GitHub half), file-history `initialPin` identity-keyed (no more wrong-commit pin from the two-tier race), agent `navigate` accepts id prefixes and 409s when no browser views the tab, `/api/file-raw` immutable cache now fires for 12-hex ids (resolved via `commit_id()` so a hex-named ref can't shadow it), launch tab never offered as closable.

## Active

- [ ] **Diff perf: validation gates + remaining default decisions** (Medium; Phases 1+2 implemented 2026-05-25) — Shipped: compute-skip decoupled from collapse limits, derived (pre-render) collapse decision, per-file deferred mounting with `revealFile()` force-mount + IO/per-frame pump + `holdViewport` re-anchoring, hide gate 1000→50k, `pnpm run bench` suite. Remaining before changing collapse-all/per-file defaults ("files arrive expanded"): run the validation gates in [specs/pierre-diffs.md](specs/pierre-diffs.md) (Chrome + a very large repo + slow hardware, Long-Task metrics, heap), decide auto-unmount of far-offscreen files (re-placeholder at ~8 viewports with measured height) once heap numbers exist, and Phase-3 items only on their numeric kill criteria. Subsumes the per-file tier of "Mega-file virtualization" below (and the former "IntersectionObserver gates computation" entry, now folded in here). Update 2026-05-26: backend diff latency measured on a very large real repository (1M+ commits) — ~0.2 s fixed per call, +~0.2 s for a 4 MB diff (fetch-bound; no per-file lazy fetch warranted locally) — and the Phase-3 trigger was measured NOT met on fast hardware (expand-all ≤62 ms to paint); what remains open is the unmeasured validation gates (slow hardware, heap, Cmd+F, annotations at scale, SSH) and the default-flip decisions.
- [ ] **OplogPanel visual diff** (Medium, needs design) — Enter → `jj op show` expansion shipped. Remaining: visual diff between op snapshots (tree-at-op-A vs tree-at-op-B). `jj op show -p` is diff-from-prev-op; comparing two ARBITRARY ops needs either `--at-op` file-show at both ops (which files?) or op-restore+snapshot (invasive). **Design question: what's the CUJ?** "Show me the working-copy diff between these two ops" wants `jj diff --from <rev> --to <rev>` where both revs resolve at their respective ops — that's not a thing jj supports directly. Closest: two-cursor compare like FileHistoryPanel, but comparing `@` at different ops instead of a file at different revisions.

## Deferred (explicit — don't do unless conditions change)

- [ ] **Bombadil rough edges** (Low) — CI wired and green on GHA (e2e.yml, 60s/PR). Spec covers log/branches/merge views + the diff/cursor/mode/focus trap-detection properties; `noModalTraps` extends to `.doc-view` and `.sym-card`. Remaining: (a) headless throughput is screenshot-capture-bound — observed ~3-5 actions in 60s, so `eventually().within(N,"seconds")` properties barely get exercised; no step-count flag in `bombadil test` yet, and headed mode produces blank screenshots in the bundled Chromium. (b) Doc-mode entry (the "Doc" button in DiffFileView has no stable test selector — just `.btn.btn-sm`) and symbol-peek (⌘+hover — Bombadil's `PressKey: {code}` has no modifier flags) aren't in the action pool, so the `.doc-view`/`.sym-card` arms of `noModalTraps` are vacuous until those gaps close. (c) No mutation-coverage spec (Enter/n/d excluded — would degrade the fixture mid-run; needs a `jj op restore`-on-loop fixture). Fixture gotcha doc'd in `e2e/bombadil/fixture.sh`: don't set `JJ_RANDOMNESS_SEED` in multi-commit fixtures (fixed seed → identical fresh-WC change_ids → accidental N-way divergence).
- [ ] **fast-check for pure functions** (Small, independent of Bombadil) — `buildKeepPlan` DONE (200-run property over generated shapes, divergence.test.ts:801). `planTake` round-trip + `reconstructSides` serialize→parse round-trip DONE (merge-surgery.test.ts — the `serializeJjConflict` inverse is the Snapshot-style `serialize()` helper there). The parser round-trips cleanly; the planTake sweep surfaced the blank-line separator data-loss class, fixed 2026-08 by moving the position model to line ranges — sweeps are now blank-inclusive with empty regions at 300–500 runs. Remaining target: `diffBlocks` (classic LCS properties — non-overlap, ordering, blocksToLineSets coverage). No browser, no compat risk.
- [ ] **`createMutationGate()` factory** (Deferred — net-neutral) — Adversarial review (2026-04 architecture pass) found singleton shape is WRONG (cross-tab bleed: tabs = separate repos). Factory shape is correct but moves ~10 lines App→file, doesn't reduce coupling (still passed as prop), doesn't structurally enforce lock (components can still call api.X direct). `onfilesaved={loadLog}` survives regardless. Revisit if a 3rd consumer beyond DiffPanel+mergeController appears.
- [ ] **`createListPicker()` for BookmarkModal/GitModal shell** (Deferred — break-even) — Shared shell is ~25 lines (previousFocus/query/index/tick-focus/index-clamp/close). Factory ~35 lines + 2×10 integration ≈ break-even at 2 consumers. CommandPalette is 3rd candidate but has submenu/breadcrumb. Do when a 3rd plain list-picker modal appears.
- [ ] **Flat `api` object at ~50 methods** (Deferred) — bookmark sub-family (7 methods) is the strongest namespace case. Pure helpers don't belong in api.ts. Hold until next expansion.
- [ ] **Watcher struct does 5 things** (Deferred — testability already achieved) — Decomposition would add 3 lifecycle owners needing coordinated shutdown for zero new testability. Revisit only if a third `OpHeadsWatcher` impl appears.
- [ ] **Annotations repo-partitioning** (Trivial) — `annotations/{changeId}.json` — changeId is jj-random (~2^128 space), collision across repos is negligible but semantically wrong.
- [ ] **UI consistency long-tail** (Low, from the 2026-05-08 code-read audit; (b) close-button drift and (c) autocomplete `:hover` DONE 2026-08) — (a) no `--radius-*` scale: `border-radius` is `3px`×~55 / `4px`×~25 / `6px`×~9 + scattered 2/5/8/10/12/14; introduce `--radius-sm/md/lg` (3/4/6) — mechanical, the 3-vs-4 drift is sub-perceptible so low urgency. (d) `.btn-link` primitive: still 1 consumer (`FileSelectionPanel .file-select-action`, plus `CommandPalette .palette-crumb` as a 2nd candidate) — add when a clear 2nd-3rd appears.
- [ ] **SSH stdin/stdout multiplexing protocol** (Complex) — one persistent SSH session, commands + responses over a framed protocol. Only worth it if port-forward isn't an option. `--remote` stays viable for quick-peek; heavy use → port-forward.

## Small features

- [ ] **Doc-mode: unread-comment badge** (Small) — agent POSTs while doc-mode is closed accumulate silently. `GET /api/doc-comments/summary` → `{path: openCount}` map; DiffFileView's `Doc` button shows a count badge. Needs a "last-seen" watermark per file in localStorage so the count is *unread*, not total.
- [ ] **Doc-mode: re-anchor on Accept** (Small) — `acceptSuggestion` could `captureAnchor` at the post-replace range and pass it to `resolveComment`, so the persisted anchor points at the replacement and a fresh `import_` lands on it. Current behavior: resolved suggestions are filtered from the orphan drawer, so this is cosmetic (the highlight position after reload).
- [ ] **Doc-mode: anchor on link/image metadata** (Medium) — `buildTextMap` only walks text nodes, so URLs/alt-text are invisible to `refind`. Agents anchoring near `[text](url)` must use the link text. Could include href/src as ghost segments (with a separate `toPM` that maps them to the link mark's range), but adds complexity for an edge case `agent_api.md` already documents.
- [ ] **Doc-mode: `.excalidraw` image embeds** (Medium) — `![](foo.excalidraw)` could render via `excalidraw-render.ts` the same way `pm-mermaid.ts` handles mermaid fences. Needs an `image` NodeView that detects `.excalidraw` src, fetches via `/api/file-show`, calls `renderExcalidrawSVG`, falls through to `<img>` for everything else.
- [ ] **Multi-line description wrap in RevisionGraph** (Small, opt-in) — Push N `isDescLine` rows instead of 1 in the `flatLines` $derived (RevisionGraph.svelte); `contGutter` already produces pure-`│` continuation so GraphSvg, the 18px row invariant, virtualization, and hover-by-`entryIndex` all work unchanged. Only open question is `wrapDesc()`: char-count word-boundary split (~10 LOC, width-stable, fine for ASCII) is the v1; if someone asks for CJK/bidi-correct or pixel-exact reflow-on-split-drag, that's [@chenglou/pretext](https://github.com/chenglou/pretext) — `prepare()` once per description + `layout()` per resize is its designed hot path, gives exact `lineCount` matching browser wrap without DOM measurement. Gate behind `config.maxDescLines` (default 1 = today's single-line ellipsis).

- [ ] **Tab context menu: Rename / Forget workspace entries** (Small, issue #30 residue) — v1.36.0 shipped the tab `◇N` / right-click menu (open/switch/add workspace, recover stale, Close tab/group) and removed the toolbar dropdown. Not yet on that menu: per-workspace Rename… and Forget (still reachable via the RevisionGraph `◇ ws@` badge right-click and Cmd+K). AppShell would need per-tab-id mutation fetches so they hit the clicked tab, not the active one.
- [ ] **Manager-level `tabs-changed` SSE** (Small, issue #30 follow-up) — background-tab stale dots currently only refresh on active-tab stale-edge / switch / open. A rebase in tab A that stales workspace tab B doesn't fire A's `stale-wc`, so B's dot lags. `Watcher.setStale` already edge-detects; have it also notify TabManager (an `OnTabsChange`-style hook now exists for the session file — reuse the shape), which broadcasts on a host-level `/events` route AppShell subscribes to. Cheaper fallback: coarse `refetchTabs()` poll while `tabs.length > 1`.
- [ ] **Routable URLs: multi-tab + live URL** (Small–Medium, issue #35 follow-up) — `?change=`/`?revset=`/`&path=` (url-intent.ts) apply to the launch tab only and are stripped after applying. Follow-ups if asked for: `?repo=<path>` → AppShell finds/opens the matching tab before seeding intent; keep `?change=` live-updated via `replaceState` as the cursor moves (makes the address bar always copyable, at the cost of history noise); FileHistoryPanel/FileComparePicker deep links.
- [ ] **Per-hunk highlighting loses enclosing nested-grammar context** (Small) — DiffPanel highlights each hunk independently, so a hunk wholly inside a Svelte `<script>` (opener line not in the hunk) parses as markup text → plain, no symbol-peek tokens; same class as a hunk starting inside a block comment. `.svelte` script/style bodies are now nested TS/CSS (languages.ts `configureNesting`), so hunks that include the opener and the whole-file editor get full tokens. Fix shape: feed the parser a synthetic opener (or the enclosing block from `fileShow` when cached) for hunks whose file-level context is known. Pinned as a known-limitation test in languages.test.ts.
- [ ] **Image diff: history/compare surfaces** (Trivial, issue #20 follow-up) — `ImageDiff` renders in DiffPanel only; FileHistoryPanel/FileComparePicker have explicit from/to revisions and could pass `imageRevision`/`imageBaseRevision` to DiffFileView too (currently still the binary placeholder there). Also possible: onion-skin/difference blend modes as a third compare mode.
- [ ] **PR badge ↔ bookmark join by commit, not just name** (Small, #36 follow-up) — non-`mine` PRs join to local bookmarks by `headRefName` alone (cross-repo forks already dropped). A same-named local bookmark that ISN'T that remote branch (unpushed, reused name) would get a colleague's badge and lose "Create PR…". Add `headRefOid` to the `gh pr list --json` set and badge non-mine PRs only when it matches the bookmark's `@<remote>` target (the frontend has remote targets in the bookmark list; RevisionGraph rows would need it threaded).
- [ ] **`/api/file-raw`: client-asserted immutability** (Trivial, follow-up) — the server infers "content-addressed" from the revision's shape (`commitIdRe`, 12+ hex, wrapped in `commit_id()` on jj ≥0.31). Truer: DiffPanel/MarkdownPreview append `&immutable=1` when they passed a `commit_id` (they know), server stops sniffing — mirrors `/api/revision?immutable=1`.
- [ ] **DiffPanel: freeze effective split while a FileEditor is open** (Small) — `config.splitView` is unsafe for any writer while an inline editor is mounted (unified↔split unmounts it → buffer lost); today three sites guard it (DiffPanel.toggleSplitView confirm, App palette routing, the overlays' panel-local state). Root fix: DiffPanel derives `effectiveSplit = editingFiles.size > 0 ? frozenAtEditStart : config.splitView`, making the key safe to write from anywhere.
- [ ] **GitLab MR badges** (Medium, issue #36 phase 2) — `githubRepoFromURL` (handlers.go) is strictly `github.com`; generalize to a `forgeFromURL()→{kind,host,repo}` detector, add a `glab mr list -R <repo> -F json` path via `RunRaw` mapping `source_branch/web_url/iid/draft` onto `PullRequest`, and a per-forge `prCompareUrl`. Open question: self-hosted GitLab hostnames won't match a whitelist — needs a config `forgeHost` or `glab` probing. Verify `glab` JSON flags against current docs before coding; stay CLI-delegated (no env-var credentials).
- [ ] **File history: syntax highlight + word-diff parity** (Small–Medium, #38 follow-up) — FileHistoryPanel/FileComparePicker pass empty highlight/word-diff maps. Parity needs `highlightFile`/`computeWordDiffsForFile` extracted from DiffPanel (component-local today) into a module + a `createDiffDerivation` pair in each panel (~120 LOC).
- [ ] **Browse full tree at revision** (Medium–Large, issue #39) — nothing exists yet (no `file list` builder/endpoint/tree component). Shape: `jj file list -r <commit_id> --ignore-working-copy -T 'json(path)++"\n"'` run once per commit_id into a small server LRU → trie → `GET /api/file-tree?commit_id=&dir=` returns one directory's children (filesets can't enumerate dirs, so laziness is server-side); `FileTreePanel` overlay (createListCursor, lazy expand, fuzzy filter) → click opens `fileShow`. Measure `jj file list` on a very large repo first (tens of MB at 10^5–10^6 files) and decide a cap.
- [ ] **SSH-mode tab grouping** (Small) — `Server.repoRoot()` returns `""` when `!hasLocalFS()`, so SSH workspace tabs render solo. `readWorkspaceStore` already shows the `RunRaw cat .jj/repo` remote-read pattern; apply the same to resolve the pointer file over SSH.

- [ ] **Symbol hover: Python docstring-after-def** (Trivial, stretch) — context trimming (`trimDocComment`) keeps the comment run *preceding* a definition; Python docstrings follow the `def` line, so they need `rg -A` after-context + parsing. Only if someone misses it.
- [ ] **Symbol hover: revision-aware lookup** (Medium) — `handleSymbol` rg's the working copy regardless of which revision DiffPanel is showing. For code review ("what does this do *now*") that's right; for archaeology it shows today's def of a symbol that may have moved. Revision-aware path: `jj file list -r X --type L` → tmpdir of `jj file show` outputs → rg the tmpdir. Cost: O(files) subprocess calls; only worth it gated on `diffTarget.commitId !== workingCopyCommitId`.
- [ ] **Conflict resolution from DiffFileView** (Medium — whole-file ours/theirs SHIPPED 2026-05; per-region + both remain) — One-click `◀ Ours` / `Theirs ▶` buttons next to `⧉ Merge` in DiffFileView's conflict header (`DiffPanel.quickResolve`). Writes the FULL reconstructed side (`reconstructSides().ours/theirs`), NOT `planTake`'s incremental block surgery (no per-block tracking needed when the whole side is the answer). Resolution goes through the shared `resolveConflictFile()` strategy (conflict-resolve.ts): `@` → `api.fileWrite`; non-`@` local → `api.mergeResolve` (`jj resolve -r`, working copy never moves); non-`@` SSH → explicit `jj edit` + write fallback whose working-copy move is surfaced to the user, never silent. N-way/git-style/auto-resolved-race falls back to the 3-pane editor. **Remaining:** (a) per-REGION resolve (resolve one `<<<<<<<` block, leave others as markers) — needs raw region-range splicing, NOT exposed by reconstructSides today; (b) "both" — whole-file `ours+theirs` duplicates shared context, so it's omitted; per-region "both" via `planTakeBoth` is the right home (no longer blocked by the blank-line gap) but waits on (a). The 3-pane `⧉ Merge` editor covers both meanwhile.
- [ ] **CSS subgrid for split-view** (Medium) — left/right panes share parent `grid-template-rows: subgrid`; row-height alignment becomes structural (browser-computed once). Could replace `split-view.ts:toSplitView()`'s JS pairing. Subgrid is baseline-2023.
- [ ] **Inline comments in split-view** (Medium, conditional on demand) — currently gated out via `useInlineComposer = !splitView`. If wanted: `ResizeObserver` on each column's `.inline-review-row`, set `min-height = max(left, right)` on both via a shared CSS var so lines below stay aligned regardless of which side's card is taller.

- [ ] **`lightjj api`: per-session bearer token** (Small) — closes the `pidAlive` TOCTOU (port freed and rebound by another local process between alive-check and connect). Server writes a random token to the `0600` session file at startup, validates it on every request; CLI reads it from the same file it discovers `addr` from. The session file is now rewritten on tab churn via `sessionWriter` (session_file.go), so the token belongs in `sessionWriter.base` (written once, preserved across rewrites). Out of scope for v1 because it's a server-side change with its own backward-compat surface.
- [ ] **MCP server over `lightjj api`** (Low) — a `lightjj mcp-stdio` subcommand speaking JSON-RPC 2.0 over stdin/stdout, wrapping `doAPIRequest`. First-class tool channel for harnesses that support MCP; orthogonal to bash denylists entirely. Defer until demand — `lightjj api` covers the bash path.
- [ ] **Cross-file batch comments** (Small) — `POST /api/doc-comments/batch` is single-file; add per-item `filePath` so an agent reviewing a 12-file change makes 1 call instead of 12. Store key already includes filePath, so the handler is just a loop over `mergeDocComment`. Wait for an agent workflow to actually hit the N-call wall before building.
- [ ] **Resolve-time batch validation (annotations)** (Medium) — annotations batch (when built) should validate `path`+`line` against `api.files()` for the change_id and reject the *whole* batch on any unresolvable target. Converts silent orphans into hard errors the agent can fix. Doc-comments equivalent needs server-side `refind()` (markdown-render dependency boundary) — defer that half.
- [ ] **Repo-local config `<repo>/.lightjj.json`** (Medium) — layer between user config and CLI flags. Per-repo theme (visually disambiguate tabs), per-repo default revset (e.g. `mine()`). `mergeAndWriteConfig` and `applyPartial()` already give the merge primitives. **Blocked on a product decision**: theme is currently a per-app singleton; per-repo theme implies per-tab theme implies tab-switch flicker. Add a layer-order lock test alongside (one test that builds all layers with conflicting keys and asserts the winner per key).

## Advanced features (roadmap 2.0)

- [ ] **N-way (3+) conflict handling in merge mode** (Medium) — `reconstructSides()` returns null for >2 sides → "unsupported" message. Queue is earliest-first (option c shipped — propagation roots auto-selected, downstream copies dimmed with ↑ hint). `jj resolve --tool` errors before invoking the tool for irreducible N-way, so the only remaining option is (a) sequential 2-at-a-time frontend orchestration.
- [ ] **SSH non-`@` merge resolve** (Small) — `handleMergeResolve` 501s in SSH (matches `handleSplitHunks`). Follow-up: `RunRaw(["mktemp"])` → `Runner.WriteFile` (already pipes via stdin) → `ResolveApply` with the remote path. ~40 LOC. See [docs/design-notes/specs/2026-04-21-resolve-tool.md](docs/design-notes/specs/2026-04-21-resolve-tool.md) §Non-goals.
- [ ] **Mega-file virtualization** (Low) — manual expand of 5000-line file renders all lines. Auto-collapse at 500 + total-line collapse at 2000 mitigate. Reference shape: two-tier — IntersectionObserver per file (rootMargin ~4000px) toggles per-file `isVisible`; *within* visible files, `createWindower` (virtual.svelte.ts, already used by RevisionGraph) on the per-hunk `{#each}` renders only `scrollTop ± overscroll` lines. The per-file IntersectionObserver now exists (Phase 2 deferred mounting). The per-file tier moved to the Active "Diff perf Phase 2" item (specs/pierre-diffs.md); the within-file `createWindower` tier stays here, gated on a measured trigger (a single near-viewport file still blocking >150ms after Phase 2).
- [ ] **Search across revisions** (Medium) — `jj log -r 'description(glob:"*query*")'` or tree-grep. Needs design.
- [ ] **SSH remote repo browser** (Low) — discover repos on remote host, open as tabs.
- [ ] **Drag-and-drop rebase** (Low) — drag revision onto destination. Inline keyboard rebase already covers the CUJ.
- [ ] **LSP-in-FileEditor** (Complex) — hover/goto in the inline editor. Tier-1 rg-backed `GET /api/symbol` (symbol.go) already covers heuristic go-to-def in the diff view; this entry is the Tier-3 upgrade (real `gopls`/`tsserver` over JSON-RPC, type-aware hover). FileEditor side is `@codemirror/view`'s `hoverTooltip` (already installed). The hard part is multi-revision semantics — LSP servers want a real FS, DiffPanel shows commit X's content.

## Known non-goals

Kept here so they don't get re-proposed.

- **Modal-union for App.svelte** — of 9 booleans only 5 are real modals, all use `bind:open` (union would need 5 getter/setter binding pairs = more code than now).
- **Keybind registry** (PaletteCommand-shaped array with `when` predicates) — adding a new mode requires auditing every `when` to add `&& !newMode.active`; current early-return-and-swallow is structurally safer.
- **`createInlineEdit()` / `createDiffSearch()` factories** — stale-guard bug + net +LOC + `set error` smell; factorizing creates "distributed monolith" with injected getters replacing closure access.
